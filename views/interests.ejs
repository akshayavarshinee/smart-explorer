<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Your Interests - Smart Explorer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 600px;
            width: 100%;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 2.5em;
        }

        .header p {
            color: #666;
            font-size: 1.1em;
            line-height: 1.5;
        }

        .group-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
            text-align: center;
        }

        .group-info h3 {
            color: #495057;
            margin-bottom: 5px;
        }

        .group-info .group-name {
            color: #007bff;
            font-weight: bold;
            font-size: 1.2em;
        }

        .interests-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }

        .interest-item {
            position: relative;
            background: #f8f9fa;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            user-select: none;
        }

        .interest-item:hover {
            background: #e3f2fd;
            border-color: #2196f3;
            transform: translateY(-2px);
        }

        .interest-item.selected {
            background: #e3f2fd;
            border-color: #2196f3;
            color: #1976d2;
        }

        .interest-item input[type="checkbox"] {
            position: absolute;
            opacity: 0;
            cursor: pointer;
        }

        .interest-item .icon {
            font-size: 2em;
            margin-bottom: 10px;
            display: block;
        }

        .interest-item .label {
            font-weight: 500;
            font-size: 1em;
        }

        .custom-interest {
            margin-bottom: 30px;
        }

        .custom-interest label {
            display: block;
            margin-bottom: 10px;
            color: #333;
            font-weight: 500;
        }

        .custom-interest input[type="text"] {
            width: 100%;
            padding: 12px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s ease;
        }

        .custom-interest input[type="text"]:focus {
            outline: none;
            border-color: #2196f3;
        }

        .submit-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
        }

        .submit-btn:active {
            transform: translateY(0);
        }

        .skip-link {
            text-align: center;
            margin-top: 20px;
        }

        .skip-link a {
            color: #666;
            text-decoration: none;
            font-size: 0.9em;
        }

        .skip-link a:hover {
            color: #333;
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px 20px;
                margin: 10px;
            }

            .header h1 {
                font-size: 2em;
            }

            .interests-grid {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }

            .interest-item {
                padding: 15px;
            }

            .interest-item .icon {
                font-size: 1.5em;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎯 Welcome!</h1>
            <p>Tell us what interests you so we can create the perfect travel experiences for your group.</p>
        </div>

        <div class="group-info">
            <h3>You've joined:</h3>
            <div class="group-name"><%= group.group_name %></div>
        </div>

        <form action="/interests" method="POST">
            <input type="hidden" name="group_id" value="<%= group.group_id %>">
            
            <div class="interests-