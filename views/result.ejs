<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Travel Plan Results</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">

  <div class="max-w-4xl mx-auto py-10">
    <h1 class="text-3xl font-bold mb-6 text-center">Your Travel Plan for <%= result.city %></h1>

    <div class="bg-white p-6 rounded-xl shadow-md mb-8">
      <h2 class="text-xl font-semibold mb-2">Request Summary</h2>
      <p><strong>Categories Requested:</strong> <%= result.categories_requested && result.categories_requested.join(", ") %></p>
      <p><strong>Mapped FSQ Categories:</strong> <%= result.categories_mapped && result.categories_mapped.join(", ") %></p>
    </div>

    <div class="bg-white p-6 rounded-xl shadow-md">
      <h2 class="text-xl font-semibold mb-4">Places Found</h2>

      <% if (result.places && result.places.length > 0) { %>
        <ul class="space-y-4">
          <% result.places.forEach(place => { %>
            <li class="p-4 border rounded-lg shadow-sm bg-gray-50">
              <h3 class="text-lg font-bold"><%= place.name %></h3>
              <% if (place.location) { %>
                <p class="text-gray-700">
                  <%= place.location.address || "" %>
                  <%= place.location.locality ? ", " + place.location.locality : "" %>
                  <%= place.location.region ? ", " + place.location.region : "" %>
                </p>
              <% } %>
              <% if (place.categories && place.categories.length > 0) { %>
                <p class="text-sm text-gray-600">
                  Categories: <%= place.categories.map(c => c.name).join(", ") %>
                </p>
              <% } %>
            </li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-gray-600">No places found. Try adjusting your search criteria.</p>
      <% } %>
    </div>
  </div>

</body>
</html>
